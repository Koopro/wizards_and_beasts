/**
 * Player transformation and morph system.
 * Manages form registry, size profiles, transformation configs, and transition state.
 * Public API entry points: {@code FormSystemAPI} and {@code SizeSystemAPI}.
 */
package at.koopro.wizardsandbeasts.form;
